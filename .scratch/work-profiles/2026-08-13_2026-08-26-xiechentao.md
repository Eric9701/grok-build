# 工作画像 · xiechentao@myai.tech

- 区间：2026-08-13 ~ 2026-08-26（近 14 天，含今天）
- Report User：`xiechentao` / xiechentao@myai.tech（查询名 `xiechengtao` 无记录，按最接近的 userId 匹配）
- 支路：单人
- 数据源：Admin API `http://10.218.220.237:22255/atlas/admin/api/task-reports`
- 样本：aggregate 全量；明细 74 条（limit=500，已覆盖该用户区间全部任务）
- 口径：一行一条子代理任务，不是一轮对话。身份来自报文体，不是 JWT。

## 一句话人设

两日集中突击的业务详设者：单仓 `csa`、单模型 `arch-kimi-for-coding`、主走 `grok-build-plan`，用 Role 3 打磨「特规津贴」详细设计，产物偏文档；几乎没有失败，取消发生在库初始化/YAPI 与 Role 4 前端原型。

## 活跃与成效

| 任务 | 成功 | 失败 | 取消 | 成功率 | Tokens | 产物 |
|---|---|---|---|---|---|---|
| 74 | 72 | 0 | 2 | 97.3% | 1.20 亿 | 86 |

- 区间内全站约 18 人、1577 任务。此人任务数 74（约 4.7%），token 约 14.3%——单任务 token 明显偏高，和 Role 3 长设计波次一致。用量对照，不是绩效结论。
- `status`：`completed` 72、`cancelled` 2、失败 0。
- 时段：14 天里 **只有 8 月 20、21 两天**有任务（周四 38、周五 36），周末为 0。`startedAt` UTC+8 集中在 10–15 点，峰值 13 点（18 条），无深夜高峰。
- 耗时：中位约 2.0 分钟，p90 约 21 分钟，最长约 47 分钟（Role 3「特规津贴详细设计」）。
- 取消 2 条，都有标题：「执行数据库初始化和YAPI同步」（general-purpose）、「前端原型与Smoke测试实现」（Role 4，约 0.4 分钟即取消）。没有 error 成簇。
- 客户端版本全部 `0.2.130`。

## 工作方式

| Agent | 任务 | 产物 | Tokens | 均耗时 |
|---|---|---|---|---|
| grok-build-plan | 68 | 86 | 1.20 亿 | 5.3 分钟 |
| atlas-sdd:3-program-design-agent | 3 | 0 | 27 万 | 40.1 分钟 |
| general-purpose | 2 | 0 | 5 万 | 7.1 分钟 |
| atlas-sdd:4-software-engineer-agent | 1 | 0 | 0 | 0.4 分钟（取消） |

- 91.9% 任务落在 `grok-build-plan`。全部 86 个产物也记在 plan 上；Role 3 三条长任务产物为 0（详设文本可能写在会话里，或产物算在 plan）。
- 用过 SDD Role 3（详设）和一次 Role 4（取消）。未见 Role 1 / 2 / explore。
- 4 个父会话、10 个子会话：有拆代理，但主量仍在 plan。

## 模型

| Catalog ID | 任务 | 产物 | Tokens | Routing Name |
|---|---|---|---|---|
| arch-kimi-for-coding | 74 | 86 | 1.20 亿 | （空） |

- **单模型依赖**：区间内只有这一个 Catalog ID，`modelRouting` 全空。排行按 Catalog ID。

## 产出与项目

- 产物密度 1.16 个/任务；kind：doc 54、code 30、other 2——文档多于代码。
- 扩展名：md 43、js 18、sql 7、py 6。
- 项目簇：cwd **全部** `E:\CMS_WS\csa`（74 / 74）。无 `worktree_path`。

## 任务主题

**说明：** 74 条里 68 条 `description` 仅为 `<user_query>`。主题只来自其余 6 条有标题任务，不是全量。

| 主题 | 条数（有标题样本） |
|---|---|
| 特规津贴详细设计（含按章节 / review 修正） | 3 |
| 数据库初始化与 YAPI 同步 | 2 |
| 前端原型与 Smoke 测试 | 1 |

有标题样本与 Role 3 三条长任务对齐，主线是「特规津贴」详设。

## 建议

1. **Role 4 这一跳没落地。** 详设已走 Role 3，实现任务（前端原型 / Smoke）被很快取消。若还要出代码，值得把取消当成流程中断，而不是能力问题。
2. **模型已经收束。** 全程 `arch-kimi-for-coding`，配额简单；Routing Name 全空，Admin 上看不到路由对照。
3. **`description` 占位与全站 plan 同源。** 68/74 为 `<user_query>`，主题只能从 6 条标题推断。修 CLI 上报口径后，这类突击波次会更好读。
