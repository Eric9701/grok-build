# 工作画像 · dingmaoju@myai.tech

- 区间：2026-08-13 ~ 2026-08-26（近 14 天，含今天）
- Report User：`dingmaoju` / dingmaoju@myai.tech
- 支路：单人
- 数据源：Admin API `http://10.218.220.237:22255/atlas/admin/api/task-reports`
- 样本：aggregate 全量；明细 48 条（limit=500，已覆盖该用户区间全部任务）
- 口径：一行一条子代理任务，不是一轮对话。身份来自报文体，不是 JWT。

## 一句话人设

工作日白天、只走 `grok-build-plan` 的 Python 实现者：模型以 `kimi-for-coding` 为主并夹杂 GLM / DeepSeek，cwd 在 `gitProject`、用户主目录和「AI安全」之间切换，产物几乎全是 code。

## 活跃与成效

| 任务 | 成功 | 失败 | 取消 | 成功率 | Tokens | 产物 |
|---|---|---|---|---|---|---|
| 48 | 41 | 7 | 0 | 85.4% | 4550 万 | 66 |

- 区间内全站约 18 人、1585 任务。此人任务数 48（约 3.0%），token 约 5.4%。用量对照，不是绩效结论。
- `status`：`completed` 41、`error` 7、取消 0。
- 时段：14 天里有任务的日期为 8/14（17）、8/17（9）、8/18（1）、8/20（11）、8/24（9）、8/25（1）。周末为 0。`startedAt` UTC+8 集中在 9–18 点，峰值 10 点（15 条），无深夜高峰。
- 耗时：中位约 0.4 分钟（大量短任务），p90 约 15 分钟，最长约 **208 分钟**（一条 `grok-build-plan` completed）。
- 失败 7 条全部是 `grok-build-plan`，`description` 均为占位符 `<user_query>`。没有有标题的失败可聚类——不宜读成「做砸了 7 个明确任务」。
- 客户端从 `0.2.126` 升到 `0.2.130`（130：21，127：12，126：10，129：5）。

## 工作方式

| Agent | 任务 | 产物 | Tokens | 均耗时 |
|---|---|---|---|---|
| grok-build-plan | 48 | 66 | 4550 万 | 7.4 分钟 |

- **100%** 任务落在 `grok-build-plan`。未见 explore、general-purpose、SDD Role 1–4。
- 17 个父会话、17 个子会话（一对一），几乎不拆多种子代理。

## 模型

| Catalog ID | 任务 | 产物 | Tokens | Routing Name |
|---|---|---|---|---|
| kimi-for-coding | 31 | 60 | 4271 万 | （空） |
| glm-5.2 | 8 | 6 | 213 万 | （空） |
| deepseek-v4-flash | 7 | 0 | 59 万 | （空） |
| qwen3.8-max | 2 | 0 | 6 万 | （空） |

- 主模型是 `kimi-for-coding`（65% 任务、91% 产物）。另外试了 GLM、DeepSeek、Qwen；后两者 0 产物。`modelRouting` 全空。排行按 Catalog ID。

## 产出与项目

- 产物密度 1.38 个/任务；kind：code 62、doc 4——代码为主。
- 扩展名：py 58、md 4、js 2、toml 2。
- 项目簇（cwd 末段）：

| 项目 | 任务 |
|---|---|
| gitProject | 19 |
| Users/dingmaoju（用户主目录） | 18 |
| AI安全 | 9 |
| qa-security-scan / `C:\` | 2 |

无 `worktree_path`。18 条落在用户主目录，工作区可能经常没落到具体仓库。

## 任务主题

**数据不足。** 48 / 48 条 `description` 均为 `<user_query>`，没有可聚类的任务标题。不能从 prompt 补（默认不读原文）。

## 建议

1. **先把工作目录落到项目仓。** 近四成任务 cwd 是 `C:\Users\dingmaoju`，和 `gitProject` / `AI安全` 并列，画像里的「项目」会被家目录稀释。
2. **description 占位比别人更彻底。** 全是 `<user_query>`，主题无法还原；和全站 plan 上报口径是同一类问题。
3. **实现路径只有 plan。** 若任务里确有详设/探查，可显式拆 Role 3 / explore；当前 7 次 DeepSeek 任务 0 产物，更像试模型而不是交付波次。
